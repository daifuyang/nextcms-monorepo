/* declare namespace API {
  // 定义登录参数
  type LoginParams = {
    account: string;
    password: string;
    loginType: 'account' | 'email' | 'phone';
    phoneType: 'password' | 'sms';
  };

  // 系统全局响应
  type Response<T> = {
    code: 0 | 1;
    message: string;
    data: T;
  };
}
 */